#pragma once
#include <iostream>
#include <string>
#include <vector>
#define UINT32 unsigned int
using namespace std;

class A
{
public:
	int getadd(int a, int b);
	vector<string> generateParenthesis(int n);
private:
	int a;
	int b;
};
